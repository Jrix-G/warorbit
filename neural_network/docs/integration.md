# Integration

Le package est autonome. L'intégration au projet principal se fait uniquement par import optionnel.

Si l'environnement de jeu expose des objets compatibles, utiliser l'adaptateur `GameState` du package.
Sinon, les tests et le training minimal restent exécutables avec des états synthétiques.

