# Run 2026-05-10

## Changements code depuis dernier run
- `train_noop_penalty_coef`: 1.5 → 0.0 (pénalité passive contradictoire retirée)
- `entropy_coef_start`: 0.065 → 0.10 (re-exploration)
- `dense_reward_clip`: 0.25 → 0.40
- `stage1_pool` élargi: greedy×2, starter×4, distance×2
- PPO clipping (eps=0.2, 3 epochs), advantage normalization
- `--learning-rate` default 0.00006
- `--eval-episodes` default 192

## Commande à lancer

```bash
PYTHONPATH=/home/jason/warorbit python neural_network_gpu/scripts/run_gpu.py --resume-checkpoint /home/jason/warorbit/runs/gpu_20260509_180503/best.npz --batch-size 128 --workers 20 --n-players 2 --learning-rate 0.00003 --ppo-epochs 2 --run-name gpu_2026_05_10_$(date -u +%H%M%S)
```

Resume depuis best 0.547 (run initial), pas 0.667 (politique passive vs pool faible).

## Surveillance
- `clip_frac` cible <0.30. Si >0.40 stable → baisser lr ou ppo-epochs.
- `entropy` doit descendre lentement, pas exploser. Cible 1.5–2.5.
- `noop` à l'éval doit baisser progressivement <0.50.
- `winrate` éval (192 ep): variance ±0.10 acceptable.

## Switch 4p après convergence 2p
```bash
PYTHONPATH=/home/jason/warorbit python neural_network_gpu/scripts/run_gpu.py --resume-checkpoint /home/jason/warorbit/runs/<run-2p>/best.npz --batch-size 128 --workers 20 --n-players 4 --target-winrate 0.45 --learning-rate 0.00002 --ppo-epochs 2 --run-name gpu_4p_$(date -u +%Y%m%d_%H%M%S)
```

Target 4p réaliste: 0.40–0.55 (random=0.25).
Baisse de winrate au début du stage 2 attendue (~0.20-0.30).
